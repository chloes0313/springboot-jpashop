spring:
  datasource:
    url: jdbc:mysql://192.168.1.60:3306/jpashop?useUnicode=true
    username: chloe
    password: justluv^*^324
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

  output:
    ansi:
      enabled: ALWAYS

logging:
  level:
    org.hibernate.SQL: debug
#  org.hibernate.type: trace #스프링 부트 2.x, hibernate5
#  org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6
